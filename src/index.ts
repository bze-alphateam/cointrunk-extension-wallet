// CoinTrunk Wallet — placeholder entry point.
//
// The real extension (MV3 manifest, background service worker, popup UI) lands
// in later tickets (BUS-10 onward). This module only exists so the project has
// a source root to type-check against while the skeleton is being set up.

export const APP_NAME = 'CoinTrunk Wallet';
